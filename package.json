{
  "name": "soksak-contract-git",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "The git domain contract (soksak-spec-plugin-git): its text and its acceptance suite. Ships no runtime artifact \u2014 consumed as a dev-dependency by the units it governs.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "gate": "bash scripts/gate.sh"
  },
  "soksakRelease": {
    "kind": "contract",
    "id": "soksak-contract-git",
    "repository": "https://github.com/soksak-ai/soksak-contract-git",
    "manifest": "release.json"
  }
}
