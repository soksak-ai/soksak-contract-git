// An implementer that declares the contract and does nothing about it. The acceptance suite must
// score it zero on every command case; if it does not, the suite is not scoring the contract.
export default {
  activate() {},
  deactivate() {},
};
